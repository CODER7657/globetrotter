# Dependency engine ranges are advisory: some transitive packages demand a
# newer Node patch than the team is standardised on. Our own requirement is
# enforced by "engines" in package.json.
engine-strict=false
auto-install-peers=true
